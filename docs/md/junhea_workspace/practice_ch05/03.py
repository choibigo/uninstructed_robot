print(all([1, 2, abs(-3)-3])) ## False가 나온다. abs(-3)-3 = 3 - 3 에서 0이 되므로 False가 된다. all은 요소중에 단 하나라도 False가 있으면 False를 출력한다.
print(chr(ord('a')) == 'a') ## True가 나온다. 'a'문자를 아스키코드로 그 아스키코드를 다시 문자로 바꾸면 그대로 'a'가 나오기 때문이다.